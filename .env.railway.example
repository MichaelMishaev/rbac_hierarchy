# ========================================
# RAILWAY PRODUCTION ENVIRONMENT VARIABLES
# ========================================
# Copy these to Railway Dashboard → Variables
# ========================================

# ========================================
# DATABASE (Railway PostgreSQL)
# ========================================
# Auto-set by Railway if using Railway PostgreSQL service
# If using external DB, set manually
DATABASE_URL=postgresql://user:password@host:port/database
DATABASE_URL_POOLED=postgresql://user:password@host:port/database?pgbouncer=true

# ========================================
# NEXTAUTH CONFIGURATION
# ========================================
# CRITICAL: Generate a new secret for production!
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=REPLACE_WITH_GENERATED_SECRET

# Set to your Railway app URL (e.g., https://your-app.railway.app)
NEXTAUTH_URL=https://your-app.railway.app

# ========================================
# APPLICATION URLS
# ========================================
# Set to your Railway app URL
NEXT_PUBLIC_APP_URL=https://your-app.railway.app

# ========================================
# NODE ENVIRONMENT
# ========================================
NODE_ENV=production

# ========================================
# OPTIONAL: EMAIL CONFIGURATION
# ========================================
# Replace MailHog with real SMTP for production
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_FROM=noreply@yourdomain.com

# ========================================
# OPTIONAL: REDIS CONFIGURATION
# ========================================
# If using Railway Redis service (for sessions, caching)
# REDIS_URL=redis://default:password@host:port

# ========================================
# NOTES
# ========================================
# 1. Never commit this file to git (already in .gitignore)
# 2. Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# 3. Update all URLs to match your Railway domain
# 4. Railway auto-sets DATABASE_URL if using Railway PostgreSQL
# 5. Manually add DATABASE_URL_POOLED (same as DATABASE_URL + ?pgbouncer=true)
