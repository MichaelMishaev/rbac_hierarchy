# Railway/Nixpacks configuration
# Forces Node.js 20 for production builds

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["cd app && npm ci"]

[phases.build]
cmds = ["cd app && npm run build"]

[start]
cmd = "cd app && npm run start"
